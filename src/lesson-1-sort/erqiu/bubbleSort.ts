import { CompareAction, SwapAction } from '../yuan/commonTypes'

async function bubbleSort(length: number, isLessThan: CompareAction, swap: SwapAction) {
  // write your code here
  // for comparing
  //
  // const compareResult = await isLessThan(i, j);
  // for swapping items
  //
  // await swap(i, j)
}

export default bubbleSort
